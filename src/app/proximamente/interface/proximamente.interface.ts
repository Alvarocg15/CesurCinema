export interface Proximamente {
  proximamente_id: string;
  proximamente_titulo: string;
  proximamente_descripcion: string;
  proximamente_ano: number;
  proximamente_duracion: number;
  proximamente_rating: number;
  proximamente_image: string;
  proximamente_trailer: string;
}
